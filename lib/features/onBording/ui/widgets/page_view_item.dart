import 'package:flutter/material.dart';

class PageViewItem extends StatelessWidget {
  const PageViewItem({super.key, required this.bgImage, required this.image, required this.subtitle, required this.title});
final String bgImage;
final String image;
final String subtitle;
final Widget title;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
